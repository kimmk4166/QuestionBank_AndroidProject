<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aoslec.questionbank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuestionBank">
        <activity android:name=".ui.subscribe.PayDoneActivity"></activity>
        <activity android:name=".ui.subscribe.PaymentActivity" />
        <activity android:name=".ui.Video.SelectVideoActivity" />
        <activity android:name=".ui.myPage.WrongNote.WrongNoteActivity" />
        <activity android:name=".WorngNote.WrongNoteActivity" />
        <activity android:name=".ui.myPage.Statistic.StatisticActivity" />
        <activity android:name=".ui.myPage.MapAPI.MapActivity" />
        <activity android:name=".Admin.AdminActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>